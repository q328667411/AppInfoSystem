<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="dao.dictionary.DataDictionaryMapper">
    <select id="getAppStatus" resultType="DataDictionary">
        SELECT valueName,valueId FROM data_dictionary WHERE typeName='APP状态'
    </select>
    <select id="getAppFlatForm" resultType="DataDictionary">
        SELECT valueName,valueId FROM data_dictionary WHERE typeName='所属平台'
    </select>
</mapper>